services:
  app:
    build: .
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - DISPLAY=host.docker.internal:0.0
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: bash
    tty: true

